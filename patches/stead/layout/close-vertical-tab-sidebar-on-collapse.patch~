Index: src/chrome/browser/ui/views/frame/vertical_tab_strip_region_view.h
===================================================================
--- src.orig/chrome/browser/ui/views/frame/vertical_tab_strip_region_view.h
+++ src/chrome/browser/ui/views/frame/vertical_tab_strip_region_view.h
@@ -70,7 +70,8 @@ class VerticalTabStripRegionView final
   // TODO(crbug.com/465832180): Replace constant based width final max width for
   // view.
   static constexpr int kUncollapsedMaxWidth = 400;
-  static constexpr int kCollapsedWidth = 42;
+  // Keep the toolbar toggle available while removing the entire tab sidebar.
+  static constexpr int kCollapsedWidth = 0;
   // TODO(crbug.com/465833741): Determine snapping behavior.
   static constexpr int kCollapseSnapWidth =
       (kUncollapsedMinWidth + kCollapsedWidth) / 2;
Index: src/chrome/app/generated_resources.grd
===================================================================
--- src.orig/chrome/app/generated_resources.grd
+++ src/chrome/app/generated_resources.grd
@@ -10518,6 +10518,9 @@ Keep your key file in a safe place. You
       <!-- /Helium Browser Layout Strings -->
 
       <!-- Vertical Tab Strings -->
+      <message name="IDS_TOGGLE_VERTICAL_TAB_SIDEBAR" desc="The tooltip and accessible name for the button that toggles the vertical tab sidebar.">
+        Toggle Sidebar
+      </message>
       <if expr="use_titlecase">
         <then>
           <message name="IDS_SWITCH_TO_VERTICAL_TAB" desc="In Title Case: The button in context menu that switches to vertical tab view.">
Index: src/chrome/browser/ui/tabs/vertical_tab_strip_state_controller.cc
===================================================================
--- src.orig/chrome/browser/ui/tabs/vertical_tab_strip_state_controller.cc
+++ src/chrome/browser/ui/tabs/vertical_tab_strip_state_controller.cc
@@ -6,7 +6,6 @@
 
 #include <optional>
 
-#include "base/i18n/rtl.h"
 #include "base/metrics/user_metrics.h"
 #include "base/metrics/user_metrics_action.h"
 #include "base/strings/string_number_conversions.h"
@@ -358,28 +357,16 @@ void VerticalTabStripStateController::Up
 }
 
 void VerticalTabStripStateController::UpdateCollapseActionItem() {
-  const bool is_collapsed =
-      GetCollapseState() != VerticalTabStripCollapseState::kExpanded;
-  const bool should_flip_icon_direction =
-      base::i18n::IsRTL() != IsTabStripRightAligned();
-
-  const gfx::VectorIcon& icon = (is_collapsed == should_flip_icon_direction)
-                                    ? views::kMenuOpenIcon
-                                    : views::kMenuCloseIcon;
-
-  const auto& text =
-      is_collapsed ? IDS_EXPAND_VERTICAL_TABS : IDS_COLLAPSE_VERTICAL_TABS;
-
   actions::ActionItem* collapse_action =
       actions::ActionManager::Get().FindAction(kActionToggleCollapseVertical,
                                                root_action_item_);
   if (collapse_action) {
-    collapse_action->SetImage(
-        ui::ImageModel::FromVectorIcon(icon, ui::kColorIcon));
-    collapse_action->SetText(BrowserActions::GetCleanTitleAndTooltipText(
-        l10n_util::GetStringUTF16(text)));
-    collapse_action->SetTooltipText(BrowserActions::GetCleanTitleAndTooltipText(
-        l10n_util::GetStringUTF16(text)));
+    const std::u16string text = BrowserActions::GetCleanTitleAndTooltipText(
+        l10n_util::GetStringUTF16(IDS_TOGGLE_VERTICAL_TAB_SIDEBAR));
+    collapse_action->SetImage(ui::ImageModel::FromVectorIcon(
+        kDockToLeftIcon, ui::kColorIcon));
+    collapse_action->SetText(text);
+    collapse_action->SetTooltipText(text);
   }
 }
 
Index: src/chrome/browser/ui/views/frame/vertical_tab_strip_region_view_browsertest.cc
===================================================================
--- src.orig/chrome/browser/ui/views/frame/vertical_tab_strip_region_view_browsertest.cc
+++ src/chrome/browser/ui/views/frame/vertical_tab_strip_region_view_browsertest.cc
@@ -10,6 +10,7 @@
 #include "base/test/scoped_feature_list.h"
 #include "build/build_config.h"
 #include "chrome/app/chrome_command_ids.h"
+#include "chrome/app/vector_icons/vector_icons.h"
 #include "chrome/browser/profiles/profile.h"
 #include "chrome/browser/ui/actions/chrome_action_id.h"
 #include "chrome/browser/ui/animation/browser_animation_controller.h"
@@ -531,21 +532,22 @@ IN_PROC_BROWSER_TEST_F(VerticalTabStripR
   state_controller()->RequestCollapse(true);
   ASSERT_FALSE(state_controller()->IsCollapsed());
 
-  // The collapse button should be updated immediately to use the expand icon
-  // and text.
-  EXPECT_EQ(BrowserActions::GetCleanTitleAndTooltipText(
-                l10n_util::GetStringUTF16(IDS_EXPAND_VERTICAL_TABS)),
-            collapse_action->GetText());
+  const std::u16string toggle_sidebar_text =
+      BrowserActions::GetCleanTitleAndTooltipText(
+          l10n_util::GetStringUTF16(IDS_TOGGLE_VERTICAL_TAB_SIDEBAR));
+  const ui::ImageModel panel_left_icon =
+      ui::ImageModel::FromVectorIcon(kDockToLeftIcon, ui::kColorIcon);
+  EXPECT_EQ(toggle_sidebar_text, collapse_action->GetText());
+  EXPECT_EQ(toggle_sidebar_text, collapse_action->GetTooltipText());
+  EXPECT_EQ(panel_left_icon, collapse_action->GetImage());
 
   // Cancel the collapse request with an expand request.
   state_controller()->RequestCollapse(false);
   EXPECT_FALSE(state_controller()->IsCollapsed());
 
-  // The collapse button should be updated immediately to use the collapse icon
-  // and text.
-  EXPECT_EQ(BrowserActions::GetCleanTitleAndTooltipText(
-                l10n_util::GetStringUTF16(IDS_COLLAPSE_VERTICAL_TABS)),
-            collapse_action->GetText());
+  EXPECT_EQ(toggle_sidebar_text, collapse_action->GetText());
+  EXPECT_EQ(toggle_sidebar_text, collapse_action->GetTooltipText());
+  EXPECT_EQ(panel_left_icon, collapse_action->GetImage());
 }
 
 // Verify that the pinned tabs container will never be larger than the unpinned
